🎯 Course Overview

Python for Data Science - Foundational programming skills for data manipulation, analysis, and visualization using NumPy, Pandas, and plotting libraries.

📚 Key Concepts Demonstrated
### **NumPy**
| Concept | Description |
|---------|-------------|
| Array creation & indexing | 1D, 2D, nD arrays with slicing |
| Vectorized operations | Broadcasting rules & element-wise math |
| Array reshaping | `reshape()`, `transpose()`, `flatten()` |
| Statistical functions | `mean()`, `median()`, `std()`, percentiles |
| Boolean indexing | Masking & conditional selection |
| Linear algebra | `dot()`, `@` matrix multiplication |
| Random sampling | `np.random` distributions & seeds |

### **Pandas**

| Concept | Description |
|---------|-------------|
| Series & DataFrame | Creation from lists, dicts, CSV |
| Selection methods | `loc/iloc`, boolean indexing |
| GroupBy operations | Split-apply-combine aggregation |
| Data cleaning | `dropna()`, `duplicated()`, `fillna()` |
| Merging/joining | `merge()`, `concat()`, `join()` |
| Pivot tables | `pivot()`, `pivot_table()`, crosstabs |
| Time series | `resample()`, `rolling()`, `shift()` |
| String operations | `str.contains()`, `str.replace()` |

### **Visualization**

| Concept | Description |
|---------|-------------|
| Matplotlib basics | Line, scatter, bar, histogram plots |
| Seaborn stats | Boxplots, violin plots, heatmaps |
| Plotly interactive | Hover, zoom, pan, dropdowns |
| Subplots | `plt.subplot()`, `make_subplots()` |
| Color palettes | Sequential, diverging, qualitative |
| Distribution plots | Histograms, KDE, rug plots |
| Correlation matrix | Heatmaps with `corr()` |
| Custom themes | `sns.set_style()`, `plt.style.use()` |

📓 Notebooks
### **Notebooks Coverage**

| Notebook | NumPy | Pandas | Visualization |
|----------|-------|--------|---------------|
| **numpy.ipynb** | ✅ | | |
| **pandas.ipynb** | | ✅ | |
| **viz.ipynb** | | | ✅ |


🎯 Learning Outcomes
| Outcome | Benefit |
|---------|---------|
| ✅ **NumPy vectorization** | → 100x faster than loops |
| ✅ **Pandas pipelines** | → Production-ready data prep |
| ✅ **Professional visualizations** | → Stakeholder-ready charts |
| ✅ **Reproducible analysis** | → Jupyter + requirements.txt |
